import XCTest
@testable import XIBLoadableRemote

final class XIBLoadableRemoteTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(XIBLoadableRemote().text, "Hello, World!")
    }
}
